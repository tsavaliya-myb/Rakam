import { Injectable } from '@nestjs/common';

@Injectable()
export class GspAdapter {
  // TODO: abstract Masters India / ClearTax / Cygnet behind one interface
  // generate(payload): Promise<GspResponse>
  // cancel(ewbNo, reason): Promise<GspResponse>
}
